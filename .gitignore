node_modules/
*.log
.DS_Store

# Worktrees (local development only, never push)
worktrees/

# Scratch / planning files (local only, never push)
scratch-*
docs/fix-issue-*-plan.md
docs/video-script.md
.npm-cache/

# E2E run artifacts (temp DSH home, decoded logs, credentials env-file — never commit)
scripts/e2e/out/
